# Copy to .env.local (dev) / set in Vercel dashboard (prod). NEVER commit real values.
SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=      # server only
CRON_SECRET=                    # long random string; same value goes in the pg_cron job header
# Optional integrations (leave blank until you have authorized access)
MAPPLS_CLIENT_ID=
MAPPLS_CLIENT_SECRET=
WHATSAPP_TOKEN=
WHATSAPP_PHONE_NUMBER_ID=
